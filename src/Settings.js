import React from 'react';
import Form from './Form';

function Settings() {
    return (
        <div>
            <h1>User settings</h1>
            <Form />
        </div>
    );
}

export default Settings;