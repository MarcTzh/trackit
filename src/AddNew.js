import React from 'react';
import Form from './Form';

function AddNew() {
    return (
        <div>
            <h1>Add a new product to track!</h1>
            <Form />
        </div>
    );
}

export default AddNew;